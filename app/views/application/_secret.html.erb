 <% @posts.each do |post| %>
  <tr>
  	<td><%= post.sender_email %>
    <td><%= post.receiver_email %></td>
    <td><%= post.subject %></td>
    <td><%= post.content.html_safe() %></td>
  </tr>
<% end %>